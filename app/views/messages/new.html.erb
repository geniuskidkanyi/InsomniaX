<% provide(:title, 'start Conversations') %>
<div class="row">

  <div class="col s12 m4 l3">
    <ul class="collection">
      <%= mailbox_section 'inbox', @box, class: 'collection-item' %>
      <%= mailbox_section 'sent', @box, class: 'collection-item' %>
      <%= mailbox_section 'trash', @box, class: 'collection-item' %>
    </ul>
  </div>

  <div class="col s12 m8 l9">
    <%= form_tag messages_path, method: :post do %>
    <div class="input-field">
      <%= label_tag 'message[subject]', 'Subject' %>
      <%= text_field_tag 'message[subject]', nil, class: 'form-control', required: true %>
    </div>

    <div class="input-field">
      <%= label_tag 'message[body]', 'Message' %>
      <%= text_area_tag 'message[body]', nil, class: 'materialize-textarea', required: true %>
    </div>

    <div class="form-group">
      <%= label_tag 'recipients', 'Choose recipients' %>
      <%= select_tag 'recipients', recipients_options(@chosen_recipient), multiple: true, class: 'form-control chosen-it' %>
    </div>

    <%= submit_tag 'Send', class: 'btn waves-effect waves-light btn' %>
    <% end %>

  </div>
</div>
