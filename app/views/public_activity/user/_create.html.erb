<li class="collection-item avatar">
    <i class="Tiny material-icons circle  cyan accent-3">recent_actors</i>
     <span class="title medium-font"> <strong class="medium-font"><%= link_to activity.owner ? activity.owner.name : 'Admin' %></strong> </span>
<% if a.trackable %>
<p><%= link_to a.trackable.name, main_app.user_path(a.trackable) %>  is a new user to insomniax let welcome him to the family</p>
       <div class="secondary-content small-font"><%= a.created_at.strftime('%-d %B %Y') %></div>
         <% else %>
         <p>


     <strong> this article doesnt exist anymore</strong>
     </p>
     <% end %>

</li>
