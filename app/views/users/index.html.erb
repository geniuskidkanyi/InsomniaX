<% provide(:title, 'All users') %>
<h5>All users</h5>
<%= will_paginate  @user ,renderer: BootstrapPagination::Rails%>
<ul class="users">

 <%= render @users %>
</ul>
<%= will_paginate @user ,renderer: BootstrapPagination::Rails %>
