<% if current_user.heart?(micropost) %>
  <%= button_to "Unheart", unheart_path(micropost_id: micropost.id), remote: true, method: :delete  %>
<% else %>
  <%= button_to "Heart", heart_path(micropost_id: micropost.id), remote: true  %>
<% end %>
