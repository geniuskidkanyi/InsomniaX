<div class="card">
  <%= form_for [@forum_thread, @forum_post] do |f| %>

  <div class="input-field col s12">
    <%= f.text_area :body,rows: '10', id: "textarea1", class: "materialize-textarea" %>

          <label for="textarea1">Add a comment </label>
      </div>
  <div class="form-group">
    <%= f.submit "make post", class: "waves-effect waves-light btn loginButton" %>
  </div>
<% end %>
</div>
